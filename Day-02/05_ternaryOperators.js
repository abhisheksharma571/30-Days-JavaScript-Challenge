//task 14: Write a program that uses the ternary operator to check if a number is positive or negative and log the reuslt to the console.
let a = 10;
console.log(a > 0 ? `${a} is positive` : `${a} is negative`);

let b = -10;
console.log(b > 0 ? `${b} is positive` : `${b} is negative`)